<!DOCTYPE html>
<html lang="en">
  <head>

    <!-- Required meta tags always come first -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

    <!-- set up html tag -->
    <html xmlns="http://www.w3.org/1999/xhtml" xmlns:og="http://ogp.me/ns#" xmlns:fb="http://www.facebook.com/2008/fbml">

    
    
    <!-- Javascript for Analytics Charts -->
    <%= javascript_include_tag "https://www.google.com/jsapi", "chartkick" %>

    <!-- Don't think I need the following since I'm now using bootstrap 4
    <%= stylesheet_link_tag  '//maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css' %>
    -->

    <!-- Not sure what the following does, I thought I should be able to remove it since I'm no longer using turbolinks because it causes issues with google analytics & javascript in general but when I remove them, my site looks funky. they don't appear to be causing any negative side effects despite commenting out the turbolinks gem & commenting it out in the javascript assets -->
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

    <!-- Site Security (auto added from rails) -->
    <%= csrf_meta_tags %>

    <!-- Set up favicons -->
    <%= favicon_link_tag 'favicon.ico' %>
    <%= favicon_link_tag 'apple-touch-icon.png', rel: 'apple-touch-icon', type: 'image/png' %>

    

    <!-- set up google analytics -->
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
          m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-81972701-1', 'auto');
      ga('send', 'pageview');

    </script>

<%= yield :head %>

</head>
<body>
  
    <!-- (this displays the error message for devise sign_in errors)
    <div class="container">

        <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>">
        <%= value %>
      </div>
      <% end %>
    </div>
  -->

    <%= render 'layouts/fb_login' unless @disable_fb_login || current_brand || 1==1 %>

    <%= render 'layouts/brand_navigation' if (current_brand && @show_brand_nav == true) %>

    
    
            
<%= yield %>
        
 <%= render 'layouts/footer' unless (current_brand || @hide_footer == true || current_user) %>



</body>
</html>